; Use the public npm registry for this project.
; This project is open-source only (React, Vite, Tailwind) and deploys to
; GitHub Pages, so it does not need Amazon's internal CodeArtifact mirror.
; A project-local .npmrc overrides ~/.npmrc for this directory only.
registry=https://registry.npmjs.org/
